# ── Python / Django ──────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/

backend/.env
backend/staticfiles/
backend/logs/*.log
backend/media/
db.sqlite3

# ── Node / Next.js ───────────────────────────────────────────
frontend/node_modules/
frontend/.next/
frontend/out/
frontend/.env.local
frontend/.env.production.local
frontend/.env.development.local

# TypeScript incremental build cache
*.tsbuildinfo

# ── Root env ─────────────────────────────────────────────────
.env

# ── OS / Editor ──────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/

# Dump DB: chứa dữ liệu sinh viên thật (CCCD, email, SĐT) và hash mật khẩu
# tài khoản nhân viên. Repo này từng để PUBLIC — không đưa dump lên git.
# Schema thì dùng docs/schema.sql (được theo dõi, chỉ có DDL, không có dữ liệu).
*.sql
!docs/schema.sql

# Rác build lọt vào repo
encoded.txt
