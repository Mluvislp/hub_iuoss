# Django API URL — Next.js proxy /api/* đến đây (server-side)
# Dev: chạy Django trên port 8000
# Production: http://127.0.0.1:8002 (cùng server, không qua Nginx)
DJANGO_API_URL=http://127.0.0.1:8000

# Token cookie name — phải khớp với middleware.ts
HUB_TOKEN_COOKIE=hub_token
